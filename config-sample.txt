# Enter your WordPress database information here if you want your database
# to be backed up automatically before each update
DB_NAME=wp_database
DB_USER=wp_user
DB_PASS=wp_password
DB_HOST=localhost

# Enter the full path to the directory where your live WordPress install
# should end up. By default this will be ./wordpress_live
# LIVE_WORDPRESS_DIR=/full_path/to_your/wordpress_blog
